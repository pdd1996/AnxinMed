{
  "name": "@anxin/root",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r exec vitest run --passWithNoTests"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
