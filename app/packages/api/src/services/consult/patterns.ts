/**
 * 咨询守门三大正则（M3-T1 · PRD §7.5）——从 demo/server/index.js 迁移并扩充。
 *
 * 顺序即优先级：L4 emergency > L3 prohibited > L2 dosage（在回答阶段过滤）。
 * 全部纯字面量，零 I/O；服务层 guards.ts 直接消费，单测覆盖各口语变体。
 *
 * ⚠️ 迁移纪律：新增关键词必须先在单测里加对应变体，避免"看起来在防但实际漏"。
 */

/**
 * L4 紧急信号：命中 → 停止常规流程 → 引导急救（PRD §7.5.1）。
 * 覆盖：胸痛/呼吸困难/意识异常/昏迷/抽搐/大量误服/儿童误服/严重过敏/喉头紧缩/呕血/黑便/大量出血/自杀/自伤。
 */
export const EMERGENCY_PATTERN =
  /胸痛|呼吸困难|意识异常|昏迷|抽搐|大量误服|儿童误服|严重过敏|喉头紧缩|呕血|黑便|大量出血|自杀|自伤/

/**
 * L3 拒答：命中 → 拒答 + 引导开方医生（PRD §7.5.2）。
 * 覆盖停药/换药/剂量调整的口语变体（"把这个药停了"/"能不能停"/"应该吃几片"等）。
 * ⚠️ 不得覆盖正常咨询词（如"这个药通常用于什么"里的"通常"不能命中）。
 */
export const PROHIBITED_PATTERN =
  /停药|停用|把这个?药?停|药停了|停掉|停吗|停不停|能停|可以停|要不要停|换药|换成|换掉|能换|可以换|增量|减量|加量|改剂量|改成.*片|应该吃几片|能吃几片|多吃了一|吃多了一/

/**
 * L2 剂量过滤：回答中出现具体剂量/频次数字 → 过滤该句 + 附固定提示语（PRD §7.5.3）。
 * 覆盖："一日X次"/"每天X片"/"每次X mg"/"分X次"/"建议服用X"等。
 * ⚠️ 保守但宁可误杀：漏放具体剂量比误杀一般文案风险高。
 */
export const DOSAGE_OUTPUT_PATTERN =
  /一日\s*[\d.]+|每天\s*[\d.]+|每日\s*[\d.]+|每次\s*[\d.]+|分\s*\d+\s*次|每[日天][^，。;；]{0,8}\d+\s*次|[\d.]+\s*(?:mg|g|毫克|克|片|粒|滴).{0,8}(?:一日|每天|每次|每日|服用)|建议.{0,6}(?:服用|用量|剂量)/i

/**
 * 段落路由（按键取数，PRD §7.5.4）：按问题类型取说明书对应段。
 * 每条 = { key, re?, label }；re 缺省 = 兜底（永远命中）。服务层按数组顺序短路匹配。
 * 结构化取数优先于语义检索——避免 LLM 幻觉，citations 三件套可追溯。
 */
export const SECTION_ROUTES: ReadonlyArray<{
  key: string
  re?: RegExp
  label: string
}> = [
  { key: 'pharmacology', re: /药理|机制|原理|机理|起效|怎么作用|为什么有效/, label: '药理毒理段' },
  { key: 'interactions', re: /相互作用|一起|联用|同时|搭配|共同/, label: '相互作用段' },
  { key: 'adverse', re: /不良|副作用|反应|不适/, label: '不良反应段' },
  { key: 'contraindication', re: /禁忌|不能|过敏/, label: '禁忌段' },
  { key: 'components', re: /成分|辅料|含有|含什么/, label: '成份段' },
  { key: 'precautions', re: /注意|事项|小心/, label: '注意事项段' },
  { key: 'storage', re: /保存|储存|存放|存/, label: '储存说明' },
  { key: 'dosage', re: /怎么吃|怎么用|用法|用量|吃几|用几|频次/, label: '医嘱提示' },
  { key: 'indication', label: '适应症段' }, // 兜底（re 缺省）：前面都不命中时取适应症
]
